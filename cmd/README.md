# cmd/ 

Stores main applications (entrypoints) for the project (e.g. cli, gui, server, etc.).

Each subdirectory under cmd/ should contain a main.go file that defines the main package and the main function. 
